const nome = "calculadora";

const soma = (a, b) => a + b;

const sub = (a, b) => a - b;

const multi = (a, b) => a * b;

const div = (a, b) => a / b;

module.exports = {
    soma,
    sub,
    multi,
    div,
    nome
}